# Core dependencies for Skin-R1 model training (SFT + GRPO) and evaluation.
# Versions pinned to the environment used for the paper experiments
# (CUDA 12.x, Qwen2.5-VL-7B-Instruct). A GPU with >=40GB (A100) is recommended.

torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0
transformers==4.52.3
huggingface-hub>=0.22.0
trl==0.14.0
peft==0.17.0
accelerate==1.4.0
datasets==3.0.2
safetensors==0.6.2
tokenizers==0.21.4
sentencepiece==0.2.1
tiktoken==0.11.0
einops==0.8.1
numpy==1.26.4
pandas==2.3.1
pillow==11.0.0
scipy==1.16.2
wandb==0.21.3
tqdm==4.67.1

# ddx eval builder imports data_construction/training_sample_generator
openai>=1.0.0
python-dotenv

# open_r1/trainer/grpo_trainer.py (explicit import; often transitive)
packaging
